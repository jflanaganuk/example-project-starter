// Codetour
/* This exists as a test file must be present for the pipeline to pass
    This file can be deleted once tests are added
*/
import { expect, test } from '@playwright/test'

test.describe('Example Test', () => {
    test('Should be true', () => {
        expect(true).toBe(true)
    })
})
